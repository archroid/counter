<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.darkLEGEND.counter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


                <activity android:name="ir.darkLEGEND.counter.SplashScreenActivity"
                android:theme="@style/SplashScreenTheme">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />

                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>

            <activity android:name="ir.darkLEGEND.counter.WelcomeActivity"
                android:theme="@style/SplashScreenTheme" />
            <activity
                android:name="ir.darkLEGEND.counter.RegisterActivity"
                android:theme="@style/loginRegisterTheme" />
            <activity
                android:name="ir.darkLEGEND.counter.LoginActivity"
                android:theme="@style/loginRegisterTheme" />
            <activity
                android:name="ir.darkLEGEND.counter.MainActivity"
                android:theme="@style/GradientTheme1" />
        </application>

</manifest>